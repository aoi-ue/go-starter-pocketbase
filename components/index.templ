package components

templ Index(name string) {
	@DocType()
	@Head()
	@Body(pageIndex(name))
	@Login()
}
templ pageIndex(name string) {
	<h1 class="text-3xl font-bold underline">
		Hello world!
	</h1>
	<p>My name is <span class="text-xl font-bold">{ name }</span></p>
}
